
<div class="question-body">
   <%= question.content %> <div class="author-info"><%= question.author.username %> <%= format_time(question) %> ago</div>
  <div class="add-comment"><%= link_to "Add Comment", new_comment_path(comment: {user_id: current_user.id, commentable_id: question.id, commentable_type: "Question"}) %> </div>
</div>
